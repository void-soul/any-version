Rust + Tauri（`#[tauri::command]` 暴露 RPC）；Windows 平台使用 `winreg` 读写注册表、`cmd /c mklink /J` 创建目录硬链接（junction）、`netstat/tasklist/wmic/sc` 系统命令探测进程与服务；HTTP 请求使用 `reqwest`（npm/GitHub/PyPI/RSS）；目录遍历使用 `walkdir`。