接下来学习echobird、ccswitch，每个工具启动前，可以选择配置的模型列表（注意claude code肯定不能选没配置antoropic协议的模型，有的工具有内置官方模型，如codex、claude，可启动前选择是否用官方
  模型，有的工具不能配置模型，如deveco，gemini），如果选择的是代理型，那么要配置代理，学习如何为每个工具配置模型或代理，选中工具后，启动时可选择历史会话或新会话（会话列表加载方式，在echobi
  rd里有实现），新会话可选目录，历史会话自动选择目录。另外，还可以选终端工具。这也是一个很大的改动，