[7126432f] 2026-09-19 WorkDaddy DAEMON_VERSION 1.2.76（build release-1.2.76-20260919-session-fork-stream-alignment-fix）—— 技能主轴改以 WorkDaddy 为主 + 会话同步增量与成长计划落地
学习记录：
- 2026-09-17 首次把 WorkDaddy（E:\pro\other-sdk\buddy\WorkDaddy）纳入本技能（此前只有 cockpit-tools）。精读并建档 16 个功能域：账号库与加密导出导入（lib.js / secure-transfer.js）、本地导入与 .logged-out（lib.js）、无感 OAuth（daemon.js）、用量/积分（credit-request-usage / credit-history-sync / credit-usage-store / credit-segments）、Token 统计（token-stats.js）、积分轮换建议（credit-rotation.js）、签到（checkin-result / checkin-consent / daemon.js dailyCheckin）、会话列表（session-db.js）、切换与假退出（lib.js::switchTo、/api/switch、/api/logout）、会话归档 .wds v4（session-transfer.js）、auto-copy lineage 合并（daemon.js）、主账号与排序（primary-account.js）、profile 与能力矩阵（profiles.js / cdp-targets.js）、自动化任务（automation*.js + schemas/automation-package.v1.schema.json）、模型管理与 cc-switch 导入（third-party-models.js）、增强开关（inject.js + theme-*.js）。commit a0cdb0a0 / 1.2.42。
- 2026-09-19 技能主轴调整：**WorkDaddy 升为主参考，cockpit-tools 降为辅参考**（我们的产品线以 WorkBuddy/CodeBuddy 为主，WorkDaddy 是只做 WorkBuddy 的深挖实现）。SKILL.md 重写 §0/§2/§3/§4/§6：新增 §0.1 硬规则「WorkDaddy 学到的语义默认只落 WorkBuddy 分支，不得据此改 CodeBuddy CN 行为」；§2.1 改为 WorkDaddy 全量对照表、§2.2 收缩为"仅在 WorkDaddy 未覆盖时按需查阅 cockpit"；裁决规则改为「账号域以 WorkDaddy 为准，切换时序与合并模型仍以 cockpit 为准」。relearn.sh 两仓输出加主/辅标注，并对 WorkDaddy 新增 `excludes`（Linux/打包类文件）。
- 2026-09-19 增量精读 1.2.42 → 1.2.76（commits 9d672eb / 51cb3a1，共 65 个监听内文件、约 +7k 行），新增/改写契约：
  - §17 会话同步明细（`daemon.js` `syncAutoCopyLineage` / `startAutoCopyJob` / `publicAutoCopyJob`）：job 模型（total/processed/copied/skipped/partial/failed/failedItems/conflicts + details[≤500]）、队列串行化与队首 re-plan、"只处理有变化"的三分支判定（baselineAt 与 contentMtime 比较：两侧都改→conflict 不覆盖；都没改→unchanged 跳过）、`sessionContentMtime` 的 5 类落盘路径。
  - §18 会话分支 Fork（`session-fork.js` / `workbuddy-compat.js::findSessionForkSelection`）：只保留前缀切片、锚点判定要求 assistant 且 complete/isEnd/finishTime 有效、跳 harness 注入块。**用户 2026-09-19 明确否决，不移植**，仅保留契约作边界知识。
  - §19 成长计划 / Buddy 状态（`growth-daily.js`、`growth-active.js` streak 扩展、`/api/growth/*`）：`normalizeDailyProgress` 的完整视图结构、任务状态判据（含 current≥target 兜底）、AUTOMATABLE/LOCKED 任务码白名单、cat state 推导与 progress、7 个 GET + 6 个 POST 接口清单与请求头、streak 的 tiers/makeup/nextTier 与"缓存命中返回整份值"的修正。
  - §20 自动化协议 V2 / 公开任务发现 / 安全评估 / 模型选择：`SCHEMA_VERSION=2 + SUPPORTED_SCHEMA_VERSIONS={1,2}`（任务包仍只收 schema 1）、新 op `log.write`（凭据字段拒绝 + 手机号/令牌脱敏）、`state.*` 的 `ttlMs`、`session.create/send` 的 `model` 与 `normalizeAutomationModelId`、`createSafetyReviewTask` 的 7 项固定结论与"查询频率不算风险"判定纪律、`analyzeTask` 的副作用/origins 静态分析、`automation-discovery.js` 的标记发现 + tasks/*.json 扫描 + 下载地址白名单（含重定向逐跳校验）+ sha256 去重 + 缓存键 pushedAt 增量、内置任务 `buddy-travel.json`（按天幂等 + 领奖后重查 + 未领养优雅跳过 + 单账号失败不影响其他账号）。
  - 修正既有契约：§6 token-stats 新增 `dailyBreakdown`；§7 `selectRotationCandidate` 改为"当前账号更早到期就不建议换"；§8 consent 机制已整体删除；§14 路由补 `/capabilities`、`/logs/clear`、`/discovery`、`/safety-review`。
- 2026-09-19 落地到 any-version（**WorkBuddy 优先**）：
  1. **会话同步增量 + 明细**：新增 `src-tauri/src/commands/buddy/session_sync.rs`（SyncTracker / 基线落盘 `{data_dir}/buddy/session-sync/<platform>/<uid>.json` / 指纹工具）；`SessionTransferReport` 增加 `sync: SessionSyncSummary`（total/copied/skipped/partial/conflict/failed/unchanged/details[≤500]）；WorkBuddy 主库由"整库 UPDATE"改为**逐会话判定后按需 UPDATE**（指纹 = `updated_at` + 会话正文本地落盘指纹，正文缺失记 partial/contentMissing）；扩展目录合并引入 `decide_sync` 三分支（无变化跳过 / 两侧都改冲突不覆盖 / 否则应用），并改用动态列探测拼 SQL；切换结束时新增 `emit_switch_sync_progress` 一次性上报台账。前端 `BuddyPanel.tsx` 增加可折叠"会话同步明细"面板（成功/跳过/冲突/失败计数 + 逐会话原因），i18n 增 `buddy.syncTitle` 等 16 键（zh/en）。
  2. **成长计划 / Buddy 状态展示**：新增 `src-tauri/src/commands/buddy/growth.rs`（`normalize_day_progress` 纯函数 + `parse_streak` + 7 个接口 + 60 秒视图缓存与 streak 缓存 + `buddy_growth_overview` 命令，仅 WorkBuddy）；前端在签到 Tab 顶部加"成长计划"卡（任务进度条 + 任务列表 + 奖励/连续活跃 7/14/28 档 + 猫猫旅行状态与归来时间 + Buddy 解锁状态 + 盲盒/抽奖次数 + 人工任务提示），i18n 增 `buddy.growth.*` 34 键（zh/en）。
- 验证：`cargo test --no-default-features --lib` 393 passed；`npx tsc --noEmit` 无错误；新增单测 session_sync 5 + workbuddy 2 + codebuddy 2 + growth 6。
- 未落地（下次同步的起点，契约已完整写入 §14/§20 与 §D）：**自动化任务体系**（协议 V2 模型/校验/调度/执行器 + 内置派旅行任务 + 公开任务发现 + 安全评估 + 运行日志/停止 + 发送消息时选模型）与 **Token/积分/调用量多维统计**（token-stats `dailyBreakdown`、`ai_usage` 补账号与日期区间、积分按天历史 + anchor 增量 + 幂等落库）。
- 本次未改 CodeBuddy CN 的任何行为（会话同步的冲突/跳过判定因走共享引擎两侧受益，属"更安全"的等价收紧，已记入 pitfalls 26）。
- 2026-09-19 第二批落地（同一 commit 7126432f 的 §14/§20 与 §5/§6）：
  3. ~~**自动化任务体系**~~ —— **已实现后又整体删除（同日，用户否决）**。曾落地 `automation/{mod.rs,exec.rs,discovery.rs}` + 两个内置任务 + `AutomationPanel.tsx` + 59 键 i18n（Rust ~3360 行 / TSX 688 行），
     覆盖协议 V2 模型与校验、能力清单（`dom.*`/`session.*`/页面类触发标 `available:false`）、触发器与调度槽位、执行器、运行注册表与停止、导入导出、副作用分析、规则化安全评估、公开任务发现。
     **否决理由（记入 `references/workdaddy.md` §14/§20 顶部与 pitfalls 41）**：① 参考任务的真正价值集中在 `dom.*`/`session.*`/页面类触发，全部依赖向 WorkBuddy renderer 注入脚本，我们没有该链路；
     ② 剩下能跑的部分（账号遍历 + 官方接口 + 按天幂等 + 定时）与我们既有的 `auto_checkin.rs`/`auto_travel.rs` 完全重叠，等于用通用解释器重写两个已写好的调度器；③ 发现/导入/安全评估的价值依赖"社区能写出我们跑不了的任务"，体量与收益不成比例。
     删除范围：`src-tauri/src/commands/buddy/automation/`（整目录）、`AutomationPanel.tsx`、`buddy/mod.rs` 的模块声明与 `accountSwitched` 派发、`lib.rs` 的 15 个命令注册与调度器启动、`BuddyPanel.tsx` 的 Tab 与 import、i18n `buddy.tabAutomation` + `buddy.automation.*`。删除后 `cargo test` 422 → 398 全绿、`tsc` 干净。
     **结论：将来只有真正具备页面注入能力时才重新评估；在此之前不要重做。**
  4. **Token / 积分 / 调用量统计**：新增 `src-tauri/src/commands/buddy/stats.rs`。Token 统计扫本地 `~/.workbuddy/projects/**/*.jsonl`（usage 别名递归查找、`sha256:occurrence` 去重、按 日/账号/模型 分桶、账号归属记录字段→路径兜底），输出 totals/daily/models/accounts/`dailyBreakdown`，**筛选在内存用 dailyBreakdown 重算**（不重扫文件）。积分/调用量走官方 `/billing/meter/get-user-request-usage`（按账号 + 区间补缺日、今日 60 秒 TTL、历史日 `final`），JSON 缓存 + `buddy_stats_clear_cache`。UI 在「积分」Tab 新增 `CreditStatsPanel.tsx`（账号/模型/近1·7·30·90 天筛选 + 纯 CSS 柱状图 + 清缓存 + 刷新），i18n 17 键 ×2。
  5. **界面调整**：成长计划卡改为可折叠（与账号状态/日历/日志一致的折叠交互）；Buddy 面板「签到」Tab 更名「积分」（zh/en），Tab 顺序为 账号 / 会话 / 积分 / 自动化 / 设置。
- 验证（第二批）：`cargo test --no-default-features --lib` **422 passed**（新增 automation 24 + stats 5）；`npx tsc --noEmit` 无错误。
- 两批合计：`cargo test` 393 → 422（自动化删除后回到 **398**），`tsc` 全程干净；qa：Q-0134 ~ Q-0139（其中 Q-0137 因功能被否决改为 `WontFix`）。
- 仍未做（下一批候选）：会话加密归档 `.wds` v4（§11）、积分轮换建议（§7）、第三方模型 cc-switch 导入（§15）、防休眠（§16）、假退出登录（§10 语义）；自动化侧尚未接 `session.*` 的模型选择（§20.2，需注入）与 ZIP 任务包（现为 JSON 信封）。
